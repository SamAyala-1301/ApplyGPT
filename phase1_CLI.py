# phase1_cli.py
'''
from utils.parser import load_resume, load_job_description
from utils.matcher import match_score

def main():
    resume_path = "data/sample_resume.docx"
    jd_path = "data/sample_jd.txt"

    print("Loading resume and job description...")
    resume_text = load_resume(resume_path)
    jd_text = load_job_description(jd_path)

    print("Scoring match...")
    result = match_score(resume_text, jd_text)

    print("\n--- APPLYGPT MATCH REPORT ---")
    print(f"✅ Match Score: {result['score']} / 10")
    print(f"🟩 Matched Skills: {', '.join(result['matched_skills']) or 'None'}")
    print(f"🟥 Missing Skills: {', '.join(result['missing_skills']) or 'None'}")
    print(f"📌 Total Skills Considered: {result['total_required']}")

#if __name__ == "__main__":
    #main()
    '''
